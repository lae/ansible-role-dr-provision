---
- hosts: localhost
  connection: local
  roles:
    - lae.travis-lxc
  vars:
    test_profiles:
      - profile: debian-stretch
      - profile: centos-7
      - profile: ubuntu-xenial
      - profile: alpine-v3.7
